# NT Greek Endings Lab

A static web app for practicing New Testament Greek word endings.

## Local Development

```bash
npm test
npm run dev
```

Then open `http://127.0.0.1:5173/`.

## Deploy

This repository includes a GitHub Pages workflow. Push to `main`, then enable GitHub Pages with **GitHub Actions** as the source in the repository settings.
